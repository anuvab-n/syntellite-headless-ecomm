# Fail fast rather than silently hoisting: modules must declare what they import.
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=false
engine-strict=true

# The API application lives at the workspace root (src/), with packages/* holding only
# artifacts shared with the frontend. Adding deps to the root is therefore intended.
ignore-workspace-root-check=true
